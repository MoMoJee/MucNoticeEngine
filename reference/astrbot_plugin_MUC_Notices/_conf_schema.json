{
  "rss_title": {
    "description": "聚合 RSS 标题",
    "type": "string",
    "default": "中央民族大学多站点通知聚合"
  },
  "rss_max_items": {
    "description": "RSS 保留条数",
    "type": "int",
    "default": 150
  },
  "poll_interval_minutes": {
    "description": "轮询间隔（分钟）",
    "type": "int",
    "default": 240,
    "hint": "学校通知时效性不强，默认 4 小时一次即可；配合每日 AI 速览使用体验更好。"
  },
  "daily_summary_enable": {
    "description": "启用每日 AI 通知速览",
    "type": "bool",
    "default": true,
    "hint": "每天定时把过去一段时间的新通知交给大模型汇总成一条「速览」，推送到 push_targets / 已订阅会话。需要 AstrBot 已配置可用的聊天大模型。"
  },
  "daily_summary_hour": {
    "description": "每日速览推送 - 时（0-23，北京时间）",
    "type": "int",
    "default": 8
  },
  "daily_summary_minute": {
    "description": "每日速览推送 - 分（0-59）",
    "type": "int",
    "default": 0
  },
  "daily_summary_lookback_hours": {
    "description": "每日速览回溯时长（小时）",
    "type": "int",
    "default": 24,
    "hint": "汇总最近这么多小时内发布的通知。"
  },
  "daily_summary_fetch_limit": {
    "description": "每日速览补抓正文的最大条数",
    "type": "int",
    "default": 15,
    "hint": "信息门户来源自带正文；其它 web 抓取来源只有标题，速览时会额外访问原文页提取正文，此项限制额外请求数以免负载过高。"
  },
  "request_timeout_seconds": {
    "description": "HTTP 请求超时（秒）",
    "type": "int",
    "default": 20
  },
  "max_concurrent_requests": {
    "description": "最大并发请求数",
    "type": "int",
    "default": 5,
    "hint": "限制同时向目标站点发送的请求数量，避免造成服务器压力。建议 3-10。"
  },
  "muc_username": {
    "description": "信息门户账号（学号/工号，用于登录抓取需要认证的站点）",
    "type": "string",
    "default": "",
    "hint": "填写统一身份认证的用户名（学号或工号）。留空则只抓取无需登录的公开站点。"
  },
  "muc_password": {
    "description": "信息门户密码",
    "type": "string",
    "default": "",
    "hint": "填写统一身份认证的密码。密码将使用 SM2 国密加密传输，并以 Cookie 方式持久化，不会明文存储密码。"
  },
  "push_targets": {
    "description": "主动推送目标会话 ID 列表",
    "type": "list",
    "default": [],
    "hint": "在此配置需要主动推送通知的会话 ID（unified_msg_origin）。可通过 /muc_notice add_push_target 指令动态添加。"
  }
}
